//
//  OnboardingDecks.swift
//  ScixSmartCards
//
//  Created by William Bostelaar on 20/7/2024.
//

import SwiftUI

struct OnboardingDecks: View {
    var body: some View {
        ScrollView {
            Text("Create")
                .font(.system(size: 40, weight: .bold))
                .foregroundColor(Color("Text"))
                .padding(.horizontal)
                .frame(maxWidth: .infinity, alignment: .leading)
            
            Text("Create")
            
        }
        .padding()
    }
}
